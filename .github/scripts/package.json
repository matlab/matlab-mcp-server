{
    "name": "github-scripts",
    "private": true,
    "scripts": {
        "lint": "eslint .",
        "test": "vitest run",
        "test:ci": "vitest run --reporter=junit --outputFile=test-results/junit.xml --coverage --coverage.reportsDirectory=test-results/coverage"
    },
    "dependencies": {
        "marked": "^18"
    },
    "devDependencies": {
        "@eslint/js": "^10",
        "@vitest/coverage-v8": "^3",
        "eslint": "^10",
        "eslint-plugin-n": "^18",
        "vitest": "^3"
    }
}